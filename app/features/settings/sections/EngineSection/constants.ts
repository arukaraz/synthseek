export const MS = 1000;

export const ENGINE_DESCRIPTION =
  "Advanced controls for the search → download → import pipeline. Defaults are tuned to a balanced sweet spot that works well for most setups, change them only if you know what you are doing. When in doubt, leave them as-is or hit Reset to defaults on the affected card if tracks start failing after a change.";
