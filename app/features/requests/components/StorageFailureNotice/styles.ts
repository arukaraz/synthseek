import { cva } from "class-variance-authority";

export const storageFailureRoot = cva("shrink-0 px-3 pb-2 sm:px-4");

export const storageFailureBody = cva("flex flex-col gap-2");

export const storageFailureActions = cva("flex flex-wrap items-center gap-2");
